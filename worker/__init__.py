"""Container-side worker code for the mineru-runpod handler.

The handler module lives at the repo root (``handler.py``) because that's
what RunPod's serverless CMD invokes. The pieces it orchestrates live here.
"""
